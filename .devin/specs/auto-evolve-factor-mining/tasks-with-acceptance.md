# 任务清单与验收标准

> 本文件记录所有任务的详细验收标准，每个任务必须全部验收通过才能标记完成

---

## 阶段一：地基搭建（第1-2天）

### Task 1: 本地环境 + 数据库Schema

**目标**: 搭建完整的基础设施，包括本地 PostgreSQL、Redis、数据库初始化

#### 验收标准（必须全部通过）

- [ ] **验收1**: 本地 PostgreSQL 和 Redis 服务状态正常
  - 服务列表: PostgreSQL, Redis
  - 命令: `pg_isready -h localhost -p 5432` 和 `redis-cli ping`
  - 预期输出: PostgreSQL 接受连接、Redis 返回 PONG

- [ ] **验收2**: 数据库连接测试通过
  - 命令: `poetry run python -c "from backend.app.db import engine; print(engine.connect())"`
  - 预期输出: 无异常，成功连接

- [ ] **验收3**: TimescaleDB hypertable 创建成功
  - SQL验证: `SELECT hypertable_name FROM timescaledb_information.hypertables;`
  - 预期输出: 包含 `ohlcv`, `factor_values`, `equity_curve` 等表

- [ ] **验收4**: SQLite 运行时数据库创建成功
  - 验证文件: `data/runtime.db` 存在
  - 验证表: `candidates`, `trades`, `alerts` 表存在

- [ ] **验收5**: 运行单元测试 `pytest tests/test_db.py`，全部通过
  - 测试覆盖率: >80%

#### 禁止事项
- 禁止使用 mock 数据库
- 禁止跳过数据库迁移
- 禁止硬编码连接字符串

#### 依赖
- PostgreSQL 17 本地服务已安装并启动
- Redis 本地服务已安装并启动
- Poetry 虚拟环境已创建

---

### Task 2: 配置中心 + FastAPI骨架

**目标**: 搭建 FastAPI 应用骨架，配置管理，API路由结构

#### 验收标准（必须全部通过）

- [ ] **验收1**: FastAPI 应用启动成功
  - 命令: `poetry run uvicorn app.main:app --host 0.0.0.0 --port 8000`
  - 预期: 无错误启动，显示 `Application startup complete`

- [ ] **验收2**: Swagger文档可访问
  - URL: `http://localhost:8000/docs`
  - 预期: 显示所有API路由，无报错

- [ ] **验收3**: 健康检查端点工作正常
  - 命令: `curl http://localhost:8000/health`
  - 预期输出: `{"status": "healthy", "services": {...}}`

- [ ] **验收4**: 配置加载正确
  - 验证: `system.yaml` 中的12个品种配置正确加载
  - 验证: 环境变量可覆盖配置

- [ ] **验收5**: 全局异常处理工作
  - 测试: 访问不存在的端点 `/api/notexist`
  - 预期: 返回 JSON 格式的 404 错误，非 HTML 错误页

- [ ] **验收6**: 运行单元测试 `pytest tests/test_main.py`，全部通过

#### 禁止事项
- 禁止使用假配置
- 禁止硬编码端口/主机
- 禁止跳过错误处理

#### 依赖
- Task 1 完成

---

## 阶段二：因子DSL + 语义约束（第2-3天）

### Task 3: 因子注册表 + 原语族定义

**目标**: 实现8族因子原语的注册和管理

#### 验收标准（必须全部通过）

- [ ] **验收1**: 8族原语全部注册成功
  - 验证: `FactorRegistry.list_families()` 返回8个族
  - 族列表: 动量/均值回归/波动率/价量/期限结构/持仓量/微观结构/宏观映射

- [ ] **验收2**: 每族包含5-8个具体原语
  - 验证: `FactorRegistry.list_primitives("momentum")` 返回 >=5 个原语

- [ ] **验收3**: 原语函数可独立执行
  - 测试: 每个原语函数传入测试数据能正确计算
  - 示例: `ts_return([1,2,3], 1)` 返回 `[nan, 1.0, 0.5]`

- [ ] **验收4**: 原语文档完整
  - 每个原语有 docstring 说明
  - 包含输入参数、返回值、示例

- [ ] **验收5**: 运行单元测试 `pytest tests/test_factor_registry.py`，全部通过
  - 测试覆盖率: >90%

#### 禁止事项
- 禁止空实现的原语
- 禁止无文档的原语
- 禁止硬编码数据

#### 依赖
- Task 2 完成

---

### Task 4: 公式DSL + 语义约束引擎

**目标**: 实现公式解析器和语义约束验证

#### 验收标准（必须全部通过）

- [ ] **验收1**: DSL解析器工作正常
  - 输入: `"ts_mean(close, 20) / ts_mean(close, 60)"`
  - 输出: 可执行的 Python 函数

- [ ] **验收2**: 表达式树序列化/反序列化
  - 测试: 序列化后的 JSON 可反序列化回相同表达式

- [ ] **验收3**: 语义约束验证
  - C001: `ts_mean` 只能接收价格/成交量/持仓量
  - C101: 禁止 `close/volume` 除法
  - C102: 禁止 `log` 负数
  - C201: 树深度>8时适应度惩罚
  - C301: `night_gap` 必须配 `vol_regime`

- [ ] **验收4**: FamilyChecker 工作
  - 验证: 每个表达式至少包含2个不同族

- [ ] **验收5**: 运行单元测试 `pytest tests/test_dsl.py`，全部通过
  - 测试覆盖率: >90%

#### 禁止事项
- 禁止跳过约束检查
- 禁止硬编码规则

#### 依赖
- Task 3 完成

---

## 阶段三：遗传编程引擎（第3-5天）

### Task 5: DEAP遗传编程核心

**目标**: 实现基于DEAP的遗传编程进化引擎

#### 验收标准（必须全部通过）

- [ ] **验收1**: DEAP环境初始化成功
  - 验证: `EvolutionEngine` 能正确初始化 PrimitiveSet

- [ ] **验收2**: 单品种进化可运行
  - 命令: `poetry run python -m quant_engine.ops.evolve_search --symbol RB`
  - 预期: 运行10代，输出每代最佳适应度

- [ ] **验收3**: 适应度函数正确
  - 公式: `0.4*sharpe + 0.3*calmar + 0.2*ic_mean - 0.5*max_dd - 0.3*turnover - 0.2*complexity`
  - 验证: 手动计算与代码结果一致

- [ ] **验收4**: 种子保护机制
  - 验证: 前10代保留20%种子

- [ ] **验收5**: 早停机制
  - 验证: 连续5代无提升时停止

- [ ] **验收6**: 运行单元测试 `pytest tests/test_evolve.py`，全部通过

#### 禁止事项
- 禁止假进化（随机生成结果）
- 禁止硬编码适应度

#### 依赖
- Task 4 完成

---

### Task 6: 分层进化三阶段

**目标**: 实现Broad Alpha Search -> Combo Search -> Combo Tune三阶段

#### 验收标准（必须全部通过）

- [ ] **验收1**: Stage 1 (Broad Alpha Search) 运行
  - 输入: 单品种历史数据
  - 输出: IC>0.03 的因子列表
  - 约束: 树深<=4

- [ ] **验收2**: Stage 2 (Combo Search) 运行
  - 输入: Stage 1 输出的因子
  - 输出: 双因子组合，夏普>0.8
  - 约束: 跨族组合

- [ ] **验收3**: Stage 3 (Combo Tune) 运行
  - 输入: Stage 2 输出的组合
  - 输出: 参数优化后的最终策略

- [ ] **验收4**: 进化中心调度12品种
  - 验证: `EvolutionCenter` 能并行/串行调度12个品种

- [ ] **验收5**: 运行验收测试 `pytest tests/test_evolution_pipeline.py`

#### 禁止事项
- 禁止跳过阶段
- 禁止假结果

#### 依赖
- Task 5 完成

---

## 阶段四：回测引擎 + 天勤桥接（第5-7天）

### Task 7: 向量化回测引擎

**目标**: 实现Numba加速的向量化回测引擎

#### 验收标准（必须全部通过）

- [ ] **验收1**: 回测引擎可运行
  - 输入: 因子值序列 + 参数
  - 输出: 夏普/Calmar/回撤/换手/交易次数

- [ ] **验收2**: Numba JIT加速
  - 验证: 使用 `@njit` 装饰器
  - 性能: 10000条数据 < 1秒

- [ ] **验收3**: 期货开平方向正确
  - 支持: OPEN/CLOSE/CLOSE_TODAY
  - IF禁止CLOSE_TODAY

- [ ] **验收4**: 手续费模型正确
  - IF平今手续费是隔日的10倍

- [ ] **验收5**: 保证金计算正确
  - 使用 `broker_margin` 而非 `exchange_margin`

- [ ] **验收6**: 运行单元测试 `pytest tests/test_backtest.py`，全部通过

#### 禁止事项
- 禁止向量化与事件驱动结果差异>5%
- 禁止忽略手续费

#### 依赖
- Task 6 完成

---

### Task 8: 天勤桥接 + 样本切分

**目标**: 实现天勤TqSdk桥接和严格的时序样本切分

#### 验收标准（必须全部通过）

- [ ] **验收1**: 天勤桥接可运行
  - 命令: `poetry run python -m quant_engine.validation.tq_bridge`
  - 预期: 能连接TqSdk，获取历史数据

- [ ] **验收2**: 向量化与天勤结果交叉验证
  - 同一策略，两种回测方式
  - 差异<5%时通过

- [ ] **验收3**: 样本切分严格时序
  - 训练: 前6个月
  - 验证: 中间6个月
  - 测试: 后6个月
  - 严禁未来函数

- [ ] **验收4**: PBO过拟合概率计算
  - 实现CSCV算法
  - PBO<0.3视为通过

- [ ] **验收5**: DSR放气夏普计算
  - DSR>0.6视为通过

- [ ] **验收6**: 运行验收测试 `pytest tests/test_validation.py`

#### 禁止事项
- 禁止使用时序外数据
- 禁止差异>5%不告警

#### 依赖
- Task 7 完成

---

## 阶段五：候选管理 + 状态机（第7-8天）

### Task 9: 候选策略全生命周期管理

**目标**: 实现候选策略的完整状态机管理

#### 验收标准（必须全部通过）

- [ ] **验收1**: 状态机流转正确
  - 合法流转: Seed→Backtest→Paper→Deployable→Running→Degraded→Retired
  - 非法流转: 拒绝并返回错误

- [ ] **验收2**: 自动替换逻辑
  - Paper挑战Running成功时替换
  - 回撤超限时降级
  - Regime不匹配时暂停

- [ ] **验收3**: 品种独立策略池
  - 每个品种: 趋势池4/波段池4/反转池2

- [ ] **验收4**: 血缘追踪
  - 记录: 父代ID、变异算子、交叉对象、进化代数

- [ ] **验收5**: 数据库存储
  - 所有状态变更持久化到SQLite

- [ ] **验收6**: 运行单元测试 `pytest tests/test_candidate.py`，全部通过

#### 禁止事项
- 禁止非法状态流转
- 禁止丢失血缘信息

#### 依赖
- Task 8 完成

---

## 阶段六：执行层 + 手动开关（第8-9天）

### Task 10: 执行网关 + 风控监控

**目标**: 实现交易执行和实时风控

#### 验收标准（必须全部通过）

- [ ] **验收1**: 天勤下单/撤单/查询持仓
  - 集成TqSdk交易接口
  - 支持模拟账户和实盘

- [ ] **验收2**: 熔断机制
  - 连续3笔异常自动暂停
  - 单日亏损>5%全系统熔断

- [ ] **验收3**: 对账机制
  - 每秒比对系统持仓vs天勤持仓
  - 不一致时告警

- [ ] **验收4**: 实时监控
  - 单品种回撤>10%自动降级
  - 总保证金接近限额时告警

- [ ] **验收5**: 手动开关功能
  - OFF/PAPER/LIVE三态切换
  - LIVE切换需二次确认

- [ ] **验收6**: 运行单元测试 `pytest tests/test_execution.py`

#### 禁止事项
- 禁止绕过风控下单
- 禁止实盘前未模拟验证

#### 依赖
- Task 9 完成

---

## 阶段七：前端界面（第9-10天）

### Task 11-14: 前端页面实现

**目标**: 实现React前端所有页面

#### 验收标准（必须全部通过）

- [ ] **验收1**: 仪表盘页面
  - 显示12品种状态
  - 权益曲线图表
  - 实时告警列表

- [ ] **验收2**: 品种详情页面
  - 策略列表
  - 回测结果
  - 手动切换开关

- [ ] **验收3**: 进化监控页面
  - 进化进度
  - 适应度曲线
  - 候选列表

- [ ] **验收4**: 风控面板
  - 保证金使用
  - 持仓分布
  - 告警历史

- [ ] **验收5**: 前端连接后端API
  - 所有数据来自真实API
  - WebSocket实时更新

- [ ] **验收6**: 运行E2E测试 `npm run test:e2e`

#### 禁止事项
- 禁止假数据
- 禁止mock API

#### 依赖
- Task 10 完成

---

## 阶段八：部署（第10-11天）

### Task 15-17: 部署与文档

**目标**: 完整部署和文档

#### 验收标准（必须全部通过）

- [ ] **验收1**: 本地服务完整部署
  - 命令: `bat-scripts\启动中心_增强版.bat`
  - 所有服务启动正常

- [ ] **验收2**: 部署文档完整
  - 安装步骤
  - 配置说明
  - 故障排查

- [ ] **验收3**: 运维脚本
  - 启动/停止脚本
  - 备份脚本
  - 监控脚本

- [ ] **验收4**: 完整系统测试
  - 端到端测试通过
  - 12品种全部可运行

#### 禁止事项
- 禁止无文档交付
- 禁止无备份机制

#### 依赖
- Task 14 完成

---

## 执行规则

1. **每个任务开始前**: 确认验收标准，预估时间
2. **每个任务完成后**: 运行所有验收测试，全部通过才能标记完成
3. **每日结束**: 更新进度，记录阻塞问题
4. **发现假数据**: 立即回滚，重新实现
5. **验收失败**: 禁止提交，修复后再试

---

**最后更新**: 2025-05-16
